# File Write Rules

- `raw/` is append-only archival storage.
- `extracted/` contains generated artefacts only.
- `wiki/` contains durable knowledge notes and generated indices.
- `outputs/` contains answer artefacts that may later be promoted into `wiki/`.
- `logs/` and `cache/` are disposable operational layers.
