# Hooks

Store automated checks or explicit hook specifications here when repeated guardrails should run consistently instead of relying on memory.

Promote a pattern into this folder when the same pre-flight, post-change, safety, or validation check keeps recurring and should become an explicit repo guardrail.
