# Roles

Store specialised role cards here when a recurring kind of subagent or review lens needs a stable scope, responsibilities, and output shape.

Promote a pattern into this folder when the same role is reused across tasks and benefits from a named brief, such as `researcher`, `debugger`, or `reviewer`.
